<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.a.checkattendance">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ActivityLogin">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HomepageOfTeacher"></activity>
        <activity android:name=".teacherMessage" />
        <activity android:name=".TeacherSettings" />
        <activity android:name=".TeacherSettingsModifypassword" />
        <activity android:name=".TeacherSettingsIdentityverifi" />
        <activity android:name=".TeacherFeedback"></activity>
        <activity android:name=".TeacherSettingsBindphone" />
        <activity android:name=".TeacherPersoninfo"></activity>
        <activity android:name=".TeacherSchool"></activity>
        <activity android:name=".TeacherStudy">

        </activity>
    </application>

</manifest>